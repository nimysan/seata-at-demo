server:
  port: 7076

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/seatademo-product-mirror?useSSL=false
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL55Dialect
    hibernate:
      ddl-auto: validate